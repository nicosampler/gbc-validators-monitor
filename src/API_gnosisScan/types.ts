export type API_GnosisScan_blockRewards = {
  blockNumber: string;
  timeStamp: string;
  blockMiner: string;
  blockReward: string;
  uncleInclusionReward: string;
};

export type API_GnosisScan_blockNumberByDate = string;
